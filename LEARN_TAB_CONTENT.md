# Learn Tab Full Content Structure

The learn tab needs:

1. Section navigation buttons (5 buttons)
2. Strategy section - WholeNumberStrategy component
3. Cycles section - MarketCycle component + session info
4. Glossary section - Trading terms + mechanics + achievements
5. Ranking section - How ranking works
6. Tips section - Trading do's and don'ts

All content is in learn/page.tsx starting from line 58 to line 591.

The content should be copied exactly but using `learnSection` state instead of `activeSection` and `setLearnSection` instead of `setActiveSection`.
