{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd whole-number-miniapp/backend && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd whole-number-miniapp/backend && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
